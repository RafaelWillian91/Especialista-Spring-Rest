O Lombok é uma biblioteca do Java que ajuda a reduzir o código boilerplate (aquele código repetitivo e chato de escrever, como getters, setters, equals, hashCode, toString, construtores etc.).

👉 Ele funciona usando anotações que, em tempo de compilação, geram esse código automaticamente, sem você precisar escrever tudo manualmente.

Exemplos comuns:

@Getter e @Setter → gera automaticamente os métodos getters e setters.

@Data → gera getters, setters, equals, hashCode, toString e até um construtor para os atributos obrigatórios.

@NoArgsConstructor, @AllArgsConstructor → gera construtores sem argumentos e com todos os argumentos.

@Builder → cria o builder pattern para facilitar a criação de objetos.

@Value → cria objetos imutáveis (tudo final e só getters).

@Slf4j → cria um logger pronto para uso (sem precisar declarar manualmente).

Vantagens:

Menos código repetitivo → classes mais limpas.

Facilita manutenção.

Foca mais na lógica do domínio, menos em "código cerimonial".

Cuidados:

Leitura do código: quem não conhece Lombok pode estranhar, porque abre a classe e não vê getters/setters escritos.

Dependência: você fica atrelado à biblioteca.

Ferramentas: IDEs modernas (Eclipse, IntelliJ, VS Code) já entendem Lombok, mas às vezes é preciso instalar plugin.