spring.datasource.url=jdbc:mysql://localhost:3306/algaFoodESR?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

#pede para o Spring gerar o DDL (as instru��es SQL de cria��o/altera��o de tabelas) se a implementa��o JPA suportar
spring.jpa.generate-ddl=true

# ATEN��O: N�O USAR EM PRODU��O!
# Essa configura��o recria o schema do banco a cada start da aplica��o
#Para produ��o, normalmente se deixa apenas o ddl-auto=validate (ou none) e remove o generate-ddl=true,
# j� que o ideal � usar uma ferramenta de migra��o de banco como Flyway ou Liquibase.
spring.jpa.hibernate.ddl-auto=create


# ATEN��O: N�O USAR EM PRODU��O!
spring.jpa.show-sql=true